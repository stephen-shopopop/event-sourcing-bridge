/**
 * Conversion factor from seconds to milliseconds.
 *
 * @constant {number}
 * @default 1000
 */
export const SECONDS_TO_MS = 1e3;

/**
 * Default interval in milliseconds for worker polling.
 *
 * @constant {number}
 * @default 1000
 */
export const DEFAULT_INTERVAL = SECONDS_TO_MS;
